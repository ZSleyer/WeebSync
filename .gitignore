# agent instructions stay local
AGENTS.md
CLAUDE.md
graphify-out

# dependencies / build
node_modules/
frontend/dist/
backend/weebsync

# editor / debugger
.vscode/
.idea/
__debug_bin*
*.test

# runtime data
*.db
*.db-*
data/
.env
cookies.txt

# sftp test fixtures (real server structure - never commit)
tools/sftp-test/index.json

# plexeval eval binary (build artifact)
/backend/plexeval

# design-system build artifacts (styles.css is a copy of the app's build)
design-system/dist/
design-system/styles.css
design-system/node_modules

# design-sync: staged converter, generated bundle, machine state
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# e2e screenshots + report (regenerated per run)
e2e-shots/

# the audit borrows playwright from wherever it is installed
tools/ui-audit/node_modules
